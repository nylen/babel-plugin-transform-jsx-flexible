<CustomTag1 />;

<CustomTag2 />;

<namespaced.CustomTag2 />;

<namespaced.rather.deeply.CustomTag3 />;

<namespaced
	.rather
	.deeply
	.CustomTag3
>
	<WithSomeWhitespace because="why not" />
</namespaced
	.rather
	.deeply
	.CustomTag3
>;

<Component prop="foo" />;
